rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /eventHub/appState {
      // event-hub は現状 Firestore を未認証で直接利用しているため、
      // いまは単一ドキュメント appState のみに権限を絞る。
      allow read, write: if true;
    }
  }
}
